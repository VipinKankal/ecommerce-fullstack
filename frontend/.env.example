# Copy the value you need into `.env.local`, `.env.development.local`, or
# deployment environment variables. A single frontend build uses one API origin
# at a time, and that origin may be either HTTP or HTTPS.

# Local development example
REACT_APP_API_BASE_URL=http://localhost:8080

# Deployed HTTPS example
# REACT_APP_API_BASE_URL=https://api.your-domain.com

# Production build requirement:
# `npm run build` will fail if REACT_APP_API_BASE_URL is missing
# or points to localhost/127.0.0.1/0.0.0.0.

# Demo fallback is development-only and disabled in production builds.
# Set true only when you explicitly want catalog demo fallback during local dev.
REACT_APP_ENABLE_MARKETPLACE_DEMO_FALLBACK=false
