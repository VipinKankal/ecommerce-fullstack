// Compatibility wrapper: keep the current page import stable while the
// canonical catalog implementation lives under src/customer/catalog.
export { default } from '../../catalog/pages/ProductListPage';
